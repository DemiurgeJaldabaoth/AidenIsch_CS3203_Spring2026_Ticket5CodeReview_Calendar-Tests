{
  "name": "calendar-tests",
  "version": "1.0.0",
  "description": "Unit tests for the calendar event app",
  "scripts": {
    "test": "jest --verbose"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
